FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    object      topoSetDict;
}

actions
(
    {
        name    overset;
        type    cellSet;
        action  new;
        source  cylinderToCell;
        sourceInfo
        {
            p1      (-0.1 0 0);
            p2      (0.3 0 0);
            radius  0.2;
        }
    }
    {
        name    overset;
        type    cellZoneSet;
        action  new;
        source  setToCellZone;
        sourceInfo { set overset; }
    }
);
